apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.storeName }}
  labels:
    app: {{ .Values.storeName }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: {{ .Values.storeName }}
